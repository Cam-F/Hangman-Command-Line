// My modules
var Movie = require("./Movie");

// Function
var Letter = function () {
    this.word = new Movie();
    this.movie = [];
    this.remaining = word.length;
    this.lives = 10;

    for (var i = 0; i < word.legnth; i++) {

        if (selectedMovie[i] === " ") {
            movie.push(" // ");
            remaining--;

        } else {
            movie.push(" _ ");
            movie.join(" ");
        }
    }
    console.log(movie);
}
